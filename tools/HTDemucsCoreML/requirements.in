# Direct dependencies for the vendored HTDemucs -> Core ML converter.
#
# Regenerate requirements.lock on Apple Silicon with:
#   uv pip compile --python-version 3.11.15 --python-platform aarch64-apple-darwin \
#     --generate-hashes --only-binary=:all: \
#     --output-file tools/HTDemucsCoreML/requirements.lock \
#     tools/HTDemucsCoreML/requirements.in
torch==2.8.0
torchaudio==2.8.0
demucs==4.0.1
coremltools==9.0
numpy==2.0.2
einops==0.8.2
